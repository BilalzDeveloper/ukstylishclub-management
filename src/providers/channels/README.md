# providers: channels

BroadcastChannel interface + adapters: TelegramChannel, TelegramDM, WhatsApp,
Instagram, ShopifyCollection. Each handles its own formatting + rate limits.
See docs/flows/daily-catalog.md.
